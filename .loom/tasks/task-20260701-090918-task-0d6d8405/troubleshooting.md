# 트러블슈팅

차단 이슈는 없었습니다.

주의할 점:

- 일부 커밋은 `git log --name-only` 출력에서 경로가 많거나 비ASCII 경로가 escape되어 보입니다. evidence-map 단계에서 `git-history.json`의 전체 changed_files를 source of truth로 사용해야 합니다.
- 실제 Jenkins 실패 로그와 장비 실행 로그는 레포에 없어 일부 troubleshooting 설명은 commit subject 기반 추론입니다.
